package main;
import chn.util.*;

public class VNDir {
	FileInput file = new FileInput("config.txt");
	
	/**
	 * Constructor, will instantiate with config file. If config file is missing then it'll make one
	 */
	public VNDir() {
		
	}
}
